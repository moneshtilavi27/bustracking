let pkg,DEBUG_MODE,CustomErrorHandler;_dae‍.x([["default",()=>_dae‍.o]]);_dae‍.w("joi",[["default",["pkg"],function(v){pkg=v}]]);_dae‍.w("../config/index.js",[["DEBUG_MODE",["DEBUG_MODE"],function(v){DEBUG_MODE=v}]]);_dae‍.w("../service/CustomErrorHandler.js",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);


const { ValidationError } = pkg;

const errorHandler = (err, req, res, next) => {

    let statusCode = 500;

    let data = {
        message: 'Internal server error',

        ...(DEBUG_MODE === "true" && { originalError: err })

    }

    if (err instanceof ValidationError) {
        statusCode = 422;
        data = {
            message: err.message
        }
    }

    if (err instanceof CustomErrorHandler) {
        statusCode = err.status;
        data = {
            message: err.message
        }
    }

    return res.status(statusCode).json(data);

}

_dae‍.d(errorHandler);